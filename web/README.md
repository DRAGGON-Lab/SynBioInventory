# SynBioInventory Web

React + TypeScript frontend for SynBioInventory MVP.

## Run locally

```bash
cd web
npm install
npm run dev
```

Set `VITE_API_BASE_URL` if backend is not running on `http://localhost:8000`.
