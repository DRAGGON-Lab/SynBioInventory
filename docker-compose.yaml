version: '3.9'
services:
  backend:
    build:
      context: ./server
    env_file:
      - .env
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./web
    env_file:
      - .env
    ports:
      - "3000:3000"
